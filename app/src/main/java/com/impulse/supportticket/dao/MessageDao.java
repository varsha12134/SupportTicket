package com.impulse.supportticket.dao;

import androidx.room.Dao;

@Dao
public interface MessageDao {
    /*@Insert
    void insertMessagesData(MessageModel messageModel);

    @Query("Select *from Message where ticketId = :ticketId")
    LiveData<List<MessageModel>> fetchMessageData(int ticketId);*/

}
