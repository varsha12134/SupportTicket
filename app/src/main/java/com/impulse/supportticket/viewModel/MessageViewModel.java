package com.impulse.supportticket.viewModel;

public class MessageViewModel  {
  /*  MessageRepository repository;
    int ticketId;
    private LiveData<List<MessageModel>> listLiveData;

    public MessageViewModel(@NonNull Application application) {
        super(application);
        repository = new MessageRepository(application);

    }
    public void insertMessages(MessageModel messageModel){
        repository.insertMessageDataInRoom(messageModel);
    }
    public LiveData<List<MessageModel>> fetchMessageDataFromRoom(int ticketId) {
        listLiveData = repository.getData(ticketId);
        return listLiveData;
    }*/

}
